{
  "name": "jam-deck",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "test": "npm run test -w server && npm run test -w client",
    "test:watch": "concurrently -n server,client -c blue,magenta \"npm run test:watch -w server\" \"npm run test:watch -w client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
