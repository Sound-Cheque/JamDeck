{
  "name": "@jam-deck/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --env-file=../.env --watch src/index.js",
    "start": "node --env-file=../.env src/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.7.0",
    "abletonlink": "^0.2.0-beta.0",
    "express": "^5.0.0",
    "multer": "^2.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^2.1.0"
  }
}
